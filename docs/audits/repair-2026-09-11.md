# Stability and performance repair — 2026-09-11

## Changes made

- Added shell-safe ADB invocation and validation for Quest serials, addresses, bitrate, and buffers.
- Added Quest audio/video start de-duplication, child-process error handling, graceful termination, and shutdown waits.
- Moved telemetry CSV formatting and disk writes to isolated per-session writer threads with bounded queues and batched flushing.
- Applied configured telemetry output paths and reset sampling state at session start.
- Consolidated repeated manual-mounting tracker grouping/orientation logic into `gui/src/hooks/manual-mounting.ts`.
- Lazy-loaded the Remote, Quest Capture, and Firmware Tool routes.
- Prevented Electron from continuing startup after invalid Java environment variables.
- Removed the unused tracker recovery timestamp field.

## Repair-scoped files

- `gui/electron/main/index.ts`
- `gui/electron/main/quest-audio.ts`
- `gui/electron/main/quest-capture.ts`
- `gui/src/App.tsx`
- `gui/src/components/onboarding/pages/mounting/ManualMounting.tsx`
- `gui/src/components/tracker/TrackerStatus.tsx`
- `gui/src/hooks/manual-mounting.ts`
- `server/core/src/main/java/dev/slimevr/VRServer.kt`
- `server/core/src/main/java/dev/slimevr/tracking/telemetry/TrackerTelemetryLogger.kt`

The existing untracked `TrackerRecoveryHandler.kt` was deliberately excluded from the rollback patch so unrelated user work is not deleted by rollback.

## Verification

- TypeScript: passed.
- ESLint: passed with zero warnings.
- Prettier check: passed.
- GUI tests: 109 passed, 0 failed.
- Production GUI renderer build: passed; fresh bundle includes the recovery-state compatibility fallback.
- `git diff --check`: passed.
- Backend Gradle tests/build: blocked before execution because the Gradle wrapper could not open `/Users/sirjame/.gradle/wrapper/dists/gradle-8.14.4-bin/92wwslzcyst3phie3o264zltu/gradle-8.14.4-bin.zip.lck` (`Operation not permitted`).

## Rollback

The repair-only reverse patch is stored at `docs/audits/repair-rollback-2026-09-11.patch`. It was generated from pre-edit snapshots and does not include unrelated existing worktree changes.

From the repository root, restore the pre-repair state with:

```sh
patch -p0 -R < docs/audits/repair-rollback-2026-09-11.patch
```

The pre-edit snapshots are also retained at `/tmp/slimevr-repair-baseline.IeaWhz` for recovery while this machine's temporary files remain available.

## Remaining limitation

The telemetry toggle still needs a SolarXR RPC schema extension to control a running server from the GUI. The current protocol submodule already contains unrelated uncommitted schema/generated changes, and `flatc` is not available in this environment; no generated protocol files were overwritten during this repair.

## GUI blank-screen repair

- Rebuilt the production renderer after reproducing the `TrackerRecoveryState.BRIDGING_GAP` startup crash.
- Added a compatibility-safe recovery enum fallback in `gui/src/components/tracker/TrackerStatus.tsx`; the fresh bundle contains `BRIDGING_GAP`.
- Guarded Electron status IPC sends when the renderer frame is disposed, preventing secondary send errors during reload/crash.
- The production GUI build passed after these changes.
